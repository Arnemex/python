import math
x = float(input("Введи x"))
y = float(input("Введи y"))
z = float(input("Введи z"))
s = y ** (abs(x) ** (1/3)) + math.cos(y) ** 3 *((abs(x-y) * (1+(math.sin(z) ** 2 / math.sqrt(x + y)))) / (math.e ** (abs(x-y)) + x / 2))
print(s)
